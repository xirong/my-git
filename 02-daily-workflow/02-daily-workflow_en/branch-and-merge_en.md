# Branch and Merge

English | [中文](../branch-and-merge.md)

Branches are used to isolate tasks, and merging is used to bring validated changes back to the main line.

Good branching habits can reduce conflicts, minimize unrelated changes, and make PRs easier to review.

## When to Create a Branch

This article uses `<integration-branch>` for the team's actual integration branch. It may be named `main`, `master`, or `develop`; replace it according to repository rules.

Create a branch for each independent task:

- New features
- Bug fixes
- Documentation updates
- Refactoring
- Experimental solutions
- Candidate solutions for code generated by AI Agents

Create a branch:

```bash
git switch -c feat/my-task
```

Switch back to the main branch:

```bash
git switch <integration-branch>
```

## Branch Naming Suggestions

```text
feat/github-governance-guide
fix/recover-lost-commit-example
docs/ai-native-git-workflow
hotfix/remove-broken-link
ai/review-large-repo-guide
```

For more templates, see [Branch Naming Convention](../../08-templates/08-templates_en/branch-naming-convention_en.md).

## Pre-Merge Checks

Before merging, first check. `git status --porcelain` must print nothing; if there are pending edits, preserve the state instead of switching to the integration branch or merging:

```bash
git status --porcelain
git diff --stat <integration-branch>...HEAD
git log --oneline <integration-branch>..HEAD
```

Confirm that:

- The branch only contains the current task
- The commit messages are understandable
- There are no unrelated formatting changes
- There are no secrets, temporary files, or build artifacts
- Validation has been completed

## Merge Methods

### merge commit

Retains the branch integration history, suitable for teams that need to audit the merge context.

```bash
git switch <integration-branch>
git merge --no-ff feat/my-task
```

### squash merge

Squashes multiple commits in a PR into a single commit, suitable for keeping the main branch history clean.

### rebase merge

Reapplies the commits in the PR on top of the target branch, suitable for teams pursuing a linear history.

The merge strategy should be unified according to team conventions, rather than chosen based on personal preference.

## Recommended Team Rules

- Personal branches can be committed frequently
- The main branch only accepts changes that have passed review and CI
- Confirm the branch is up to date before merging
- Split large changes into multiple small PRs
- Prioritize reverting when issues occur

## Further Reading

- [git branch official documentation](https://git-scm.com/docs/git-branch)
- [git merge official documentation](https://git-scm.com/docs/git-merge)
- [Atlassian: Git Branch](https://www.atlassian.com/git/tutorials/using-branches)
- [Team Git Workflow Guide](../../03-team-collaboration/03-team-collaboration_en/team-git-workflow-guide_en.md)
